# Optional: used at build time if your CI supports env vars.
# For Cloudflare static assets, edit public/config.json instead (no dashboard env vars needed).
VITE_API_BASE_URL=https://your-backend.example.com/api
